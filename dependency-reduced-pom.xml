<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>cyclone.games</groupId>
  <artifactId>water-sort-puzzle-solver</artifactId>
  <name>Water Sort Puzzle Solver</name>
  <version>1.1.0-SNAPSHOT</version>
  <description>An algorithm for solving "water sort puzzle" games.</description>
  <build>
    <finalName>${project.artifactId}-${project.version}</finalName>
    <plugins>
      <plugin>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>${maven-surefire-plugin.version}</version>
      </plugin>
      <plugin>
        <artifactId>maven-shade-plugin</artifactId>
        <version>${maven-shade-plugin.version}</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <transformers>
                <transformer>
                  <manifestEntries>
                    <Main-Class>cyclone.games.watersortpuzzle.solver.cli.CLI</Main-Class>
                  </manifestEntries>
                </transformer>
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>com.google.cloud.tools</groupId>
        <artifactId>jib-maven-plugin</artifactId>
        <version>${jib-maven-plugin.version}</version>
        <configuration>
          <from>
            <image>eclipse-temurin:17.0.11_9-jre</image>
          </from>
          <to>
            <image>${jib-maven-plugin.docker-image.name}</image>
          </to>
          <container>
            <mainClass>cyclone.games.watersortpuzzle.solver.cli.CLI</mainClass>
            <user>1000:1000</user>
          </container>
        </configuration>
      </plugin>
    </plugins>
  </build>
  <dependencies>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>5.11.0</version>
      <scope>test</scope>
      <exclusions>
        <exclusion>
          <artifactId>junit-jupiter-api</artifactId>
          <groupId>org.junit.jupiter</groupId>
        </exclusion>
        <exclusion>
          <artifactId>junit-jupiter-params</artifactId>
          <groupId>org.junit.jupiter</groupId>
        </exclusion>
        <exclusion>
          <artifactId>junit-jupiter-engine</artifactId>
          <groupId>org.junit.jupiter</groupId>
        </exclusion>
      </exclusions>
    </dependency>
  </dependencies>
  <properties>
    <maven.compiler.target>17</maven.compiler.target>
    <junit-jupiter.version>5.11.0</junit-jupiter.version>
    <apache-commons-cli.version>1.9.0</apache-commons-cli.version>
    <maven-surefire-plugin.version>2.22.2</maven-surefire-plugin.version>
    <maven.compiler.source>17</maven.compiler.source>
    <maven-shade-plugin.version>3.3.0</maven-shade-plugin.version>
    <jib-maven-plugin.docker-image.name>${project.artifactId}:${project.version}</jib-maven-plugin.docker-image.name>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <jansi.version>2.4.1</jansi.version>
    <jib-maven-plugin.version>3.4.1</jib-maven-plugin.version>
  </properties>
</project>
